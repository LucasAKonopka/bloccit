<div class="form-group">
  <%= f.label :rating %>
  <% if rating %>
      <%= f.select :rating, options_for_select(Rating.severities, Rating.severities[rating.severity.to_s]) %>
  <% else %>
      <%= f.select :rating, options_for_select(Rating.severities) %>
  <% end %>
</div>